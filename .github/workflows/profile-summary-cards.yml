name: GitHub-Profile-Summary-Cards

on:
  schedule:
    - cron: "0 */12 * * *"   # Runs every 12 hours
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: vn7n24fzkq/github-profile-summary-cards@release
        with:
          USERNAME: MohammadRstm
          UTC_OFFSET: 3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
